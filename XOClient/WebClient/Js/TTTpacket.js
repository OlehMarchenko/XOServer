TTTPacket = function (playerTurn, unit, buttonNumber, matrix, gameResult) {
    this.PlayerTurn = playerTurn;
    this.Unit = unit;
    this.ButtonNumber = buttonNumber;
    this.Matrix = matrix;
    this.GameResult = gameResult;
}